import fs from 'node:fs';
const wasmPath=process.argv[2]||'public/audioV2/mvpHdV2.wasm';
const bytes=fs.readFileSync(wasmPath);
const imports={env:{sin:Math.sin,cos:Math.cos,pow:Math.pow,exp:Math.exp,log10:Math.log10}};
const sr=48000;
async function inst(){const {instance}=await WebAssembly.instantiate(bytes,imports);const e=instance.exports;if(e.mvp_v2_init(sr)!==1)throw Error('init');return e;}
function signal(type='dynamic',sec=3){const n=Math.floor(sr*sec),L=new Float32Array(n),R=new Float32Array(n);for(let i=0;i<n;i++){const t=i/sr;let env=1;if(type==='dynamic'){const beat=t%0.5;env=.46+(beat<.028?1.0*Math.exp(-beat*90):0); } else if(type==='hot') env=.92; else if(type==='brick') env=1.15;let x=(.30*Math.sin(2*Math.PI*50*t)+.22*Math.sin(2*Math.PI*110*t)+.18*Math.sin(2*Math.PI*1000*t)+.12*Math.sin(2*Math.PI*3400*t)+.08*Math.sin(2*Math.PI*9000*t));let y=(.28*Math.sin(2*Math.PI*50*t+.03)+.20*Math.sin(2*Math.PI*110*t+.1)+.18*Math.sin(2*Math.PI*1000*t+.15)+.11*Math.sin(2*Math.PI*3400*t+.4)+.07*Math.sin(2*Math.PI*9000*t+.7));x*=env;y*=env;if(type==='brick'){x=Math.max(-.94,Math.min(.94,x*1.65));y=Math.max(-.94,Math.min(.94,y*1.65));}L[i]=x;R[i]=y;}return {L,R};}
async function render(opts={}){const e=await inst();e.mvp_v2_set_output_profile(opts.profile??1);e.mvp_v2_set_mode(opts.mode??1);e.mvp_v2_set_intensity(opts.intensity??.72);e.mvp_v2_set_bass_enabled(opts.bass?1:0);e.mvp_v2_set_bass_character(opts.bassChar??.5);e.mvp_v2_set_impact_enabled(opts.impact?1:0);e.mvp_v2_set_clarity_enabled(opts.clarity?1:0);e.mvp_v2_set_spatial_enabled(opts.spatial?1:0);e.mvp_v2_set_space_mode(opts.spaceMode??0);e.mvp_v2_set_personal_enabled(opts.personal?1:0);e.mvp_v2_set_personal_bass(opts.pb??0);e.mvp_v2_set_personal_presence(opts.pp??0);e.mvp_v2_set_personal_brightness(opts.pbr??0);if(opts.master)e.mvp_v2_set_master_prep(1,opts.master.sourceGainDb??1,opts.master.highpassHz??22,opts.master.lowMidDb??-.5,opts.master.presenceDb??.7,opts.master.harshnessDb??-.8,opts.master.balanceDb??0,opts.master.widthScale??1.03);if(opts.eq){e.mvp_v2_set_eq_enabled(1);for(const [idx,db] of opts.eq)e.mvp_v2_set_eq_band(idx,db);}const sig=signal(opts.signal??'dynamic',opts.sec??3);const n=sig.L.length,outL=new Float32Array(n),outR=new Float32Array(n);const max=e.mvp_v2_max_frames(),mem=e.memory,li=new Float32Array(mem.buffer,e.mvp_v2_input_l(),max),ri=new Float32Array(mem.buffer,e.mvp_v2_input_r(),max),lo=new Float32Array(mem.buffer,e.mvp_v2_output_l(),max),ro=new Float32Array(mem.buffer,e.mvp_v2_output_r(),max);for(let off=0;off<n;off+=max){const m=Math.min(max,n-off);li.fill(0);ri.fill(0);li.set(sig.L.subarray(off,off+m));ri.set(sig.R.subarray(off,off+m));if(e.mvp_v2_process(m)!==1)throw Error('process');outL.set(lo.subarray(0,m),off);outR.set(ro.subarray(0,m),off);}return {L:outL,R:outR,tp:e.mvp_v2_meter_true_peak_dbtp(),lim:e.mvp_v2_meter_limiter_gr_db(),clips:e.mvp_v2_meter_clip_count(),nans:e.mvp_v2_meter_nan_count()};}
const skip=4096;
function rms(o){let s=0,n=0;for(let i=skip;i<o.L.length;i++){s+=o.L[i]*o.L[i]+o.R[i]*o.R[i];n+=2;}return Math.sqrt(s/n)}
function db(x){return 20*Math.log10(Math.max(1e-12,x))}
function mag(o,f,side=false){let cr=0,ci=0,n=0;for(let i=skip;i<o.L.length;i++){const x=side?(o.L[i]-o.R[i])*.5:(o.L[i]+o.R[i])*.5,a=2*Math.PI*f*i/sr;cr+=x*Math.cos(a);ci-=x*Math.sin(a);n++;}return 2*Math.hypot(cr,ci)/n}
function transientRatio(o){let ts=0,tn=0,ss=0,sn=0;for(let i=skip;i<o.L.length;i++){const beat=(i/sr)%0.5,x=(o.L[i]+o.R[i])*.5;if(beat<.02){ts+=x*x;tn++;}else if(beat>.12&&beat<.32){ss+=x*x;sn++;}}return Math.sqrt(ts/Math.max(1,tn))/Math.sqrt(ss/Math.max(1,sn));}
const cases=[];function add(name,pass,data){cases.push({name,pass,data});console.log(pass?'PASS':'FAIL',name,data)}
for(const signalName of ['dynamic','hot','brick']){
 const pure=await render({mode:0,signal:signalName});const ad=await render({mode:1,intensity:.78,signal:signalName});const pw=await render({mode:2,intensity:.78,signal:signalName});
 add(`${signalName} modes differ`,Math.abs(db(rms(ad)/rms(pure)))>.25 && (Math.abs(db(rms(pw)/rms(ad)))>.35 || Math.abs(db(mag(pw,3400)/mag(ad,3400)))>.5),{P:db(rms(pw)/rms(ad)).toFixed(2),A:db(rms(ad)/rms(pure)).toFixed(2),lim:pw.lim.toFixed(2)});
 add(`${signalName} safe`,pw.clips===0&&pw.nans===0&&pw.tp<=-.35,{tp:pw.tp,lim:pw.lim});
}
const i0=await render({mode:2,intensity:0,signal:'hot'}),i1=await render({mode:2,intensity:1,signal:'hot'});add('intensity changes power',Math.abs(db(rms(i1)/rms(i0)))>.3 || Math.abs(db(mag(i1,3400)/mag(i0,3400)))>.7,{rms:db(rms(i1)/rms(i0)),pres:db(mag(i1,3400)/mag(i0,3400))});
for(const sig of ['dynamic','hot','brick']){const off=await render({mode:2,intensity:.78,signal:sig}),on=await render({mode:2,intensity:.78,bass:true,bassChar:.6,signal:sig});const ratio=db((mag(on,50)+mag(on,110))/(mag(off,50)+mag(off,110)));const level=db(rms(on)/rms(off));add(`${sig} bass adds low without collapse`,ratio>.55&&level>-.30,{ratio,level,lim:on.lim});}
const tight=await render({mode:2,bass:true,bassChar:0,signal:'dynamic'}),deep=await render({mode:2,bass:true,bassChar:1,signal:'dynamic'});add('tight deep direction',db((mag(deep,50)/mag(deep,110))/(mag(tight,50)/mag(tight,110)))>.8,{delta:db((mag(deep,50)/mag(deep,110))/(mag(tight,50)/mag(tight,110)))});
for(const sig of ['dynamic','hot','brick']){const off=await render({mode:2,intensity:.78,signal:sig}),on=await render({mode:2,intensity:.78,clarity:true,signal:sig});const ratio=db((mag(on,3400)+mag(on,9000))/(mag(off,3400)+mag(off,9000)));const level=db(rms(on)/rms(off));add(`${sig} clarity adds highs without collapse`,ratio>.55&&level>-.30,{ratio,level,lim:on.lim});}
const imp0=await render({mode:2,signal:'dynamic'}),imp1=await render({mode:2,impact:true,signal:'dynamic'});add('impact increases transient-to-sustain',db(transientRatio(imp1)/transientRatio(imp0))>.25,{delta:db(transientRatio(imp1)/transientRatio(imp0)),lim:imp1.lim});
const sp0=await render({mode:2,signal:'dynamic'}),sp1=await render({mode:2,spatial:true,signal:'dynamic'});add('spatial increases side',db(mag(sp1,1000,true)/mag(sp0,1000,true))>.6,{delta:db(mag(sp1,1000,true)/mag(sp0,1000,true))});
for(const [name,key,f] of [['personal bass','pb',50],['personal presence','pp',3400],['personal bright','pbr',9000]]){const base=await render({mode:2,personal:true,signal:'dynamic'}),pos=await render({mode:2,personal:true,[key]:1,signal:'dynamic'}),neg=await render({mode:2,personal:true,[key]:-1,signal:'dynamic'});add(`${name} positive`,db(mag(pos,f)/mag(base,f))>.8,{d:db(mag(pos,f)/mag(base,f))});add(`${name} negative`,db(mag(neg,f)/mag(base,f))<-.8,{d:db(mag(neg,f)/mag(base,f))});}
const eq0=await render({mode:1,signal:'dynamic'}),eq1=await render({mode:1,eq:[[17,6]],signal:'dynamic'});add('31band eq audible',db(mag(eq1,1000)/mag(eq0,1000))>2.5,{d:db(mag(eq1,1000)/mag(eq0,1000)),lim:eq1.lim});
const m0=await render({mode:1,signal:'dynamic'}),m1=await render({mode:1,master:{presenceDb:1.5,lowMidDb:-1},signal:'dynamic'});add('master prep audible',Math.abs(db(mag(m1,3400)/mag(m0,3400)))>.5,{d:db(mag(m1,3400)/mag(m0,3400))});
const stack=await render({mode:2,intensity:1,bass:true,bassChar:.7,impact:true,clarity:true,spatial:true,personal:true,pb:.5,pp:.5,pbr:.5,signal:'hot'});add('full stack safe',stack.clips===0&&stack.nans===0&&stack.tp<=-.35&&stack.lim<8,{tp:stack.tp,lim:stack.lim});
console.log(`\n${cases.filter(x=>x.pass).length}/${cases.length} PASS`);if(cases.some(x=>!x.pass))process.exit(1);
