name: MVP Audio Foundation R12

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "public/audio/mvpSoundModes-r12.worklet.js"
      - "src/lib/musicPlayer.ts"
      - "src/features/music/MusicMiniPlayer.tsx"
      - "src/workers/musicAudioIntelligence.worker.ts"
      - "scripts/test-audio-foundation-r12.mjs"
      - ".github/workflows/audio-foundation-r12.yml"

permissions:
  contents: read

jobs:
  validate-audio-foundation:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Validate R12 Worklet syntax
        run: node --check public/audio/mvpSoundModes-r12.worklet.js
      - name: Run R12 real-mode and Song IQ validation
        run: node scripts/test-audio-foundation-r12.mjs public/audio/mvpSoundModes-r12.worklet.js
      - name: Run production app build
        run: npm run build
