name: MVP Audio Foundation R14

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "public/audio/mvpSoundModes-r14.worklet.js"
      - "src/lib/musicPlayer.ts"
      - "src/features/music/MusicMiniPlayer.tsx"
      - "scripts/test-audio-foundation-r14.mjs"
      - ".github/workflows/audio-foundation-r14.yml"

permissions:
  contents: read

jobs:
  validate-audio-foundation:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate R14 Worklet syntax
        run: node --check public/audio/mvpSoundModes-r14.worklet.js

      - name: Run R14 mode-switch stability validation
        run: node scripts/test-audio-foundation-r14.mjs

      - name: Run production app build
        run: npm run build
